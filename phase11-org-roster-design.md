# 組織ロースター（所属）レイヤー 設計メモ

対象リポジトリ: `tad-iizuka/FirebaseRTC`
作成日: 2026-07-30
位置づけ: `brushup-plan.md` Phase11（組織階層）の後追い拡張、および
Phase12（役割と機能の整理）と合わせて検討する設計提案。**本ドキュメントは
まだ実装を伴わない設計合意のためのメモであり、方向性（後述の案C）について
ユーザー合意済み。実装着手前の詳細設計・見積もりはこれから。**

---

## 1. 背景・課題

`brushup-plan.md` 十三訂で、Phase11（組織階層）実装時に以下を確定していた。

> Room作成とRoomの組織階層への紐付けは分離する。（中略）これに伴い、
> 「ユーザー×団体の所属関係」（誰がどの団体に属するか）の設計は当面
> 不要と判断し、着手しない。

この判断自体はRoom First原則（README.md）を守る上で妥当だったが、副作用として
以下2点が5.4「洗い出された矛盾点・懸念点」に未解決のまま残っていた。

- **バッジマスタの団体単位管理が成立しない**（十九訂）：団体単位でバッジを
  出し分けるには「誰がどの団体に属するか」の判定が前提として必要だが、その
  判定手段自体が存在しない
- **招待コードの可視範囲**：admin-dashboardから招待コードを再取得する手段を
  追加しようとすると、既存の`rooms:monitor`権限（サイト管理者向け・全Room横断）
  にしか委譲先がなく、権限が過大になる

いずれも根をたどると「組織 ⇄ ユーザー」の関係がシステム上に存在しないことに
起因する。本メモはこの関係をどう補うかを検討する。

## 2. 例え：MLBのロースターとの対応

警備業界の「所属」は、MLBの球団と選手の関係に近い。

- 選手は基本的にどこかの球団に所属するが、**一時的な契約関係**であり、
  トレードで他球団に移ることも、フリーエージェント（FA）になることもある
- 所属していることと、実際に試合に出場すること（出場記録）は別レイヤーである
- 出場記録・成績は選手個人に紐づいたまま残り、移籍してもリセットされない

| MLB | 本システム | 実装状況 |
|---|---|---|
| 球団 | Company（`organizations`） | 実装済み（Phase11） |
| 選手 | Member（`uid`、メール認証） | 実装済み |
| ロースター登録 | `organizations/{orgId}/members/{uid}` | **未実装（本メモの提案）** |
| トレード | 旧団体の名簿から削除 → 新団体へ追加 | 未実装 |
| フリーエージェント | どの団体名簿にも属さないMember | 実装済み（現状は全員がこの状態） |
| 試合出場 | Room参加（招待コード経由） | 実装済み |
| 助っ人・単発参加 | Guest（匿名認証、名簿と無関係） | 実装済み |

この対応から導かれる設計上の要点は、**「所属」と「Room参加（出場）」を
別レイヤーとして分離する**ことである。

## 3. 他プロファイルとの緊張関係

「ユーザー×団体」の関係を必須かつ強い制約として実装すると、README.mdの
Target Roadmapにある他プロファイルと衝突する。

- **イベント運営・展示会・自治体等（Phase2想定）**：スタッフが当日限りの
  一時的な関係であることが多く、警備業のような安定した所属を前提にすると
  過剰設計になる
- **コンシューマー（Phase3想定）**：団体という概念自体が存在しない
- **Guestロール**：匿名認証ユーザーを「団体のメンバー」として扱うことは
  設計上ありえない（Guestは昇格導線自体を持たない方針。`brushup-plan.md`
  5.1・十三訂参照）

一方で、「一切実装しない」という現状維持では、警備業側の実務要件（団体単位の
バッジ運用、配置管理、招待コード運用）が積み残しのまま増え続ける。

## 4. 検討した選択肢と結論

| 案 | 概要 | 警備業への適合 | 他プロファイルへの影響 |
|---|---|---|---|
| A. 現状維持 | 団体⇄ユーザーの関係を持たない | 配置管理・団体単位のバッジ運用が不可 | 影響なし（元々不要） |
| B. 必須の所属関係 | 所属を正式実装し、Room参加もそこに紐づける | 実務にフィット | Temporary Relationships / Guestと衝突。過剰設計 |
| **C. 任意のロースター層（採用）** | 所属情報は追加するが、**Room入室の権限判定には使わない**。名簿・配置管理・団体スコープ機能のための付帯情報として位置づける | フィットしつつ拡張的 | 使わないプロファイルは単に空集合のまま。既存のRoom First原則を壊さない |

**→ 案Cで進めることをユーザーと合意済み。**

### 案Cの設計原則

1. **所属（ロースター）はアクセス制御の軸にしない。** Roomに入れるか・
   何ができるかは、これまで通り`rooms/{roomId}/members/{uid}`のrole
   （owner/moderator/member/guest）だけで決まる。所属情報はあくまで
   「団体管理者が自社の状況を横断的に見るための付帯情報」という位置づけ。
   Phase12で`isAnonymous`（未入室で判定可能なアカウント種別）と`role`
   （入室後のみ存在するRoom内での役割）を「統一ではなく使い分け」と整理
   したのと同じ発想で、「組織所属」と「Room role」も別軸として扱う
2. **Guestは名簿の対象外。** 匿名認証ユーザーを名簿に載せる意味がなく、
   5.1「Member昇格は対象外」方針とも整合する
3. **登録は任意。** 警備業は運用上ほぼ全員登録される想定だが、イベント
   運営で一時スタッフを都度登録するかは業態次第。名簿が空でもシステムは
   壊れない設計とする
4. **Room作成と組織階層への紐付けを分離した十三訂の方針は変更しない。**
   ただし、団体に所属するユーザーが作ったRoomの所属Site候補をUI上で
   提案する、といった**補助**は将来追加できる余地を残す（自動紐付けの
   強制ではなく選択肢の事前入力）

## 5. データモデル（案）

```
organizations/{orgId}/
  members/{uid}
    orgRole: 'admin' | 'staff'   // 団体内での役割。Room roleとは別軸
    joinedAt: timestamp
    // Room roleのような細かい権限フラグは持たせない（粗い区分のみ）
```

- `orgId`は既存の`organizations`コレクション（Phase11実装済み）のドキュメントID
- Room roleとの混同を避けるため、フィールド名・型ともに明確に分離する
  （例: `role`ではなく`orgRole`とする）
- `firestore.rules`は既存の`organizations`/`nodes`と同様、クライアント直接
  読み書きは全面拒否し、Admin SDK経由のAPIに一本化する方針を踏襲する

## 6. 動線案

### 6.1 新しい警備会社を登録・管理する人の動線

**現状（実装済み）でできること：**

1. 警備会社から利用申し込み（システム外の営業プロセス）
2. サイト管理者が`admin-dashboard`にログイン
3. `OrganizationsView.vue`から新規Companyを作成
4. 配下にBranch/Siteノードを再帰的に作成
5. 現場でRoomが作られたら、`RoomDetailView.vue`の`org-assignment`で
   該当Siteに事後紐付け

ここで止まる。箱（Company/Branch/Site）は用意できても、「誰がこの会社の
警備士か」「誰が現場責任者か」を紐付ける手段が現状存在しない。

**ロースター層を足した場合（追加提案）：**

6. サイト管理者が、その警備会社の**最初の1人（団体管理者）**を代理登録する。
   対象ユーザーは先にMember（メール認証）登録済みである必要があるため、
   実務上は「担当者にまずMemberとしてサインアップしてもらってから、
   サイト管理者が`organizations/{orgId}/members/{uid}`に
   `orgRole: 'admin'`を付与する」という順序になる（最初の1人だけは
   鶏卵問題があるため、サイト管理者の代理作業が必要）
7. 以降はその団体管理者自身が（サイト管理者を介さず）自社配下の
   Branch/Site管理・所属警備士の名簿管理を行える

**未解決の設計課題**：この動線を成立させるには、`admin-dashboard`の権限
モデルに新しいスコープが必要になる。現状は「サイト管理者（PTT運営全体を
管理）」か「Room内owner/moderator（そのRoom限定）」の2択しかなく、
「特定の`orgId`配下だけ管理できる団体管理者」という中間スコープが存在
しない。Phase12（role×操作の整理）と合わせて設計する必要がある。

### 6.2 一般警備士がシステムに入る動線

**現状（実装済み）でできること：**

1. 現場のRoom作成時、招待コードが発行される（`POST /rooms`のレスポンス
   のみ。以降どのAPIからも再取得不可という既存仕様）
2. 招待コードを警備士に共有（誰がどう共有するかは運用任せ。5.4
   「招待コードの可視範囲」の通り、admin-dashboard側からも再取得できない
   ため、実務上は現場責任者が口頭・チャット等で個別に伝える運用になっている）
3. 警備士はPTTクライアントを開き、招待コードを入力して参加
4. 認証方法としてMember（メール必須）かGuest（匿名）を選択
5. Room内でmember（任命されればmoderator/owner）として活動
6. Room解散後もMemberアカウントは永続。次のRoomに入るには再度招待コードが必要

この動線には「警備士としてシステムに登録される」ステップが存在しない。
あるRoomに招待コードで入った瞬間にそのRoomのmemberになるだけで、
「どこの会社の警備士か」という情報はどこにも残らない。

**ロースター層を足した場合（追加提案）：**

1. 団体管理者が、警備士のメールアドレス宛に「うちの団体所属である」という
   名簿エントリを作成（招待）。警備士がまだMemberアカウントを持っていなければ、
   先にMember登録を促す
2. 警備士はMemberとしてサインアップ（5.2のメール必須ルールはそのまま）、
   団体の名簿に紐づく
3. **現場のRoomに入る手順は一切変えない。** これまで通り招待コードで入る
   （Room First原則を保つため、名簿への所属とRoom参加権限は連動させない）
4. 団体管理者は名簿のおかげで「うちの所属警備士一覧」「誰が今どのSiteに
   配置されているか」を横断的に見られるようになる（既存のRoom単位の
   管理者ビューでは不可能だった視点）
5. **転職（トレード相当）**：旧団体の名簿から除名、新団体の名簿へ追加。
   Member ID自体は不変（5.2「削除しない限り永続的」の原則通り）。
   認証情報・過去のRoom参加履歴（監査ログ）は個人に紐づいたまま動かさない
6. **業界から離れる（FA・引退相当）**：全ての団体名簿から外れるだけ。
   Memberアカウント自体を消すかどうかは既存の5.2ルール（本人の退会申請、
   または管理者削除）に従う、別の話として扱う

## 7. 既存の積み残し課題への効果

案Cを導入すると、`brushup-plan.md` 5.4で保留になっている以下2点に道筋がつく。

- **バッジマスタの団体単位管理**（十九訂で「所属関係がないため成立しない」
  と結論）：本メモの名簿層ができれば前提が揃うため、Phase15（業界
  ラベリング層）着手時にこの制約を解消できる
- **招待コードの可視範囲**：現状の課題は「`rooms:monitor`権限まで招待
  コード閲覧を広げると権限が過大になる」という点だった。名簿層で
  「このSiteのstaff」という粒度が持てれば、招待コード閲覧権限を
  `rooms:monitor`のような広い管理者権限ではなく、「対象Site配下の
  staffに限定した閲覧」のような、より絞った権限として設計しやすくなる

## 8. 未確定・次に詰めるべき事項

- **団体管理者というスコープ付き権限を`admin-dashboard`の権限モデルに
  どう追加するか**（6.1の未解決課題）。Phase12（role×操作の対応表整理）
  の一部として検討する
- **最初の団体管理者の代理登録フロー**（6.1 手順6の鶏卵問題）を正式な
  APIとしてどう実装するか
- **`orgRole`の粒度**：`admin`/`staff`の2値で足りるか、現場責任者
  （Site単位の権限）のような中間ロールが必要か
- **転職・退会時の名簿更新を誰がトリガーするか**（本人申告か、団体管理者
  操作のみか）
- **Room作成時の所属Site候補の事前提案**（補助機能）の要否・優先度
- 本メモはデータモデルと動線の方向性のみを扱っており、Firestoreルールの
  詳細・監査ログとの連携（badge.grant/revokeで踏襲したパターンを流用
  できるか）は未検討

---

> 本メモは、ユーザーとの検討（MLBロースターとの対比、新規団体登録者・
> 一般警備士それぞれの動線の具体化）を経て、案C（任意のロースター層、
> Room roleとは独立したアクセス制御を持たない付帯情報）の方向性で
> 合意した内容を記録したものである。実装着手前の設計メモであり、
> `brushup-plan.md`のPhase12検討時に本メモを参照しつつ詳細設計を行う。
